# To build use command: docker compose -f docker-compose.yml -f docker-compose.build.yml build
services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    image: ivanskem/aesterial-jcup:backend-latest

  frontend:
    build:
      context: ./frontend/web
      dockerfile: Dockerfile
    image: ivanskem/aesterial-jcup:frontend-latest
